---
"koishi-plugin-chatluna-toolbox": minor
---

Add native tools for updating the bot QQ avatar and managing Koishi plugins from ChatLuna. The avatar tool now normalizes HTTP images to OneBot base64 input, and plugin management is also available through the `toolbox.plugin` Koishi command with plugin-level authority and user allowlist checks.
