## Agent skills

### Issue tracker

本仓库的 issues 和 PRD 使用 GitHub Issues 管理，仓库为 `Sor85/AAAAACAT-chatluna-plugins`，操作时使用 `gh` CLI。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五个分诊标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

使用单上下文布局：存在时读取根目录 `CONTEXT.md` 和根目录 `docs/adr/`。详见 `docs/agents/domain.md`。
